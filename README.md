# .vim

This repository contains my personal Vim configuration, allowing me to quickly and consistently set up my preferred Vim environment on a new machine.

## Installation

1. Clone this repository into your home directory (`~`).
2. Run the install script:

   ```sh
   ./install.sh
   ```
